/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configure Next.js to look for app directory in src folder
  distDir: '.next',
  // No need for custom configuration for src directory in Next.js 14+
  // Next.js automatically supports src directory structure
};

export default nextConfig;
